import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

const TestScreen = () => {
  return (
    <SafeAreaView>
      <Text> aaa</Text>
    </SafeAreaView>
  );
};

export default TestScreen;
